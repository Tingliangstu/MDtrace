MDtrace documentation
=====================

From the repository root, install the documentation dependencies and run a
strict HTML build:

   python -m pip install -r docs/requirements.txt
   python -m sphinx -W -b html docs/source docs/_build/html

The same configuration is used by Read the Docs through .readthedocs.yaml.
